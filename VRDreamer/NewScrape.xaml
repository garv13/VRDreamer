<Page
    x:Class="VRDreamer.NewScrape"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VRDreamer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
       
            
                <Button x:Name="but" Height="50" HorizontalAlignment="Stretch" VerticalAlignment="Top" Content="Finding your Location" Click="Button_Click" Background="#33918D8D"/>
                <CaptureElement Name="PreviewControl" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            
       
    </Grid>
    <Page.BottomAppBar>
        <AppBar x:Name="Appbar" IsOpen="False" Visibility="Visible" IsSticky="True">
            <StackPanel Orientation="Horizontal">
                <AppBarButton Name="NextBar" Icon="Add" Label="Add new Scrap"   />
                <AppBarButton Name="ColorSelect" Icon="Find" Label="Search in Store"/>
            </StackPanel>
        </AppBar>
    </Page.BottomAppBar>
</Page>
